import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    fundo:{
        flex:1,
        alignItems: "center",
        justifyContent: 'center',
        backgroundColor: "#36BFBF",
      },
});

export default styles;